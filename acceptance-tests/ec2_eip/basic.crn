# EC2 EIP - Basic test
#
# Tests: Elastic IP allocation with tags

provider aws {
  region = aws.Region.ap_northeast_1
}

aws.ec2.Eip {
  domain = aws.ec2.Eip.Domain.vpc

  tags = {
    Environment = 'acceptance-test'
  }
}
