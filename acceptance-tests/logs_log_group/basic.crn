# CloudWatch Logs Log Group - Basic test
#
# Tests: Log group creation with retention and tags

provider aws {
  region = aws.Region.ap_northeast_1
}

aws.logs.log_group {
  log_group_name = '/carina/acceptance-test'
  retention_in_days = 7

  tags = {
    Environment = 'acceptance-test'
  }
}
